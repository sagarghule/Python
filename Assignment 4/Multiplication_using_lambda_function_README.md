This program contains one lambda function 
which accepts two parameters and return product
of that numbers.
Lambda function is one line anonymous function
which accept multiple parameters but only one
expression.
